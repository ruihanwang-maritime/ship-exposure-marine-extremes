# Verified with Python 3.11.8 on Windows (conda).
# Only fig1 and fig3 need cartopy; everything else runs without it.
numpy==1.26.4
pandas==2.3.2
scipy==1.10.1
matplotlib==3.10.6
statsmodels==0.14.0
pyarrow==11.0.0
Cartopy==0.22.0

# Import order note: on Windows/conda, pyarrow must be imported before cartopy,
# which loads GEOS/PROJ libraries that shadow one of pyarrow's. fig1 and fig3
# pin the order explicitly.
